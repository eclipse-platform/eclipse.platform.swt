name: TestTest
concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

on:
  push:
    branches: [ test_SyntevoAlex ]
  pull_request:
    branches: [ test_SyntevoAlex ]

env:
  GH_TOKEN: ${{secrets.GITHUB_TOKEN}}

jobs:
  build:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Create Gist
        shell: bash
        run: |
          gh gist create --desc "eclipse.platform.swt.tests" --public
